<%= form_for @question do |f| %>
  <%= f.label :title %><%= f.text_field :title %><%= @question.errors[:title].join(',') %><br />
  <%= f.label :body %><%= f.text_area :body %><br />
  <%= f.submit %>
<% end %>